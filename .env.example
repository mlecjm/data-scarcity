## Copy to ``.env`` and adjust as needed.

## Reproducibility seed used by every CLI unless overridden
DS_SEED=42

## Override paths if running inside Docker / a different layout
# DS_DATA_DIR=/workspace/data
# DS_MODELS_DIR=/workspace/models
# DS_REPORTS_DIR=/workspace/reports
